# = Class: need
#
# provides a series of defines allowing to check existence of certain
# resources like packages or users
#
# need::package { 'wget': }
# need::user { 'plack': }
#

define need() {
}
